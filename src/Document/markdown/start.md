# 使用

## 基本的组件使用

```TypeScript
import {Button} from 'fake-ui'

export default {
	components: {
		Button
	}
}
```

## Vue单文件组件

代码示例:

```vue

<template>
  <div>
    <Button>默认按钮</Button>
  </div>
</template>
```